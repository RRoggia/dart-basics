var numbers = [1, 2, 3];

main() {
  var integers = [1, 2, 3];
  // integers.add("a"); //not assignable
  //integers.add(20.0); //not assignable
  integers.add(4);
  print(integers);

  List<num> numbers = [1, 2, 3];
  numbers.add(20.0);
  print(numbers);

  List<double> doubles = [10.0, 1, 30.0];
  doubles.add(30);
  print(doubles);

  // spread operator

  // collection if

  // collection for
}
